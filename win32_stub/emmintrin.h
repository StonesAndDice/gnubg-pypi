/*
 * Stub for MinGW: winnt.h includes this after x86intrin.h. Empty guard so
 * the real emmintrin.h is never pulled in (we already stubbed x86intrin.h).
 */
#ifndef WIN32_STUB_EMMINTRIN_H
#define WIN32_STUB_EMMINTRIN_H
#endif
